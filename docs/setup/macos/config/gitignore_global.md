# `~/.gitignore_global` file settings

Don't forget to active it on `~/.gitconfig` file

```bash
.idea
.DS_Store
.irbrc
node_modules/
.elixir_ls/
.vscode/
.dynamodb
.dynamodb/
pnpm-lock.yaml
.iex.exs
```
